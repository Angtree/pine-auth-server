#!/usr/bin/env node
var global = require('./global.js');
var debug = require('debug')('NodeStudy');
var app = require('../src/app');

app.set('port', global.port);

var server = app.listen(app.get('port'), function() {
  debug('Express server listening on port ' + server.address().port);
});

//var crypto = require('crypto');
//var cipher = crypto.createCipher('aes128', 'password');
//cipher.update('hello, world', 'utf8', 'binary');
//var encryptedBinary = cipher.final('binary');
//console.log(encryptedBinary);
//
//var decipher = crypto.createDecipher('aes128', 'password');
//decipher.update(encryptedBinary, 'binary', 'utf8');
//var decryptedUtf8 = decipher.final('utf8');
//console.log(decryptedUtf8);